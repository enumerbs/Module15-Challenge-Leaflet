import plateBoundariesData from "../data/PB2002_boundaries.json" with { type: 'json' };
// Attach the plateBoundariesData to the browser 'window' object as a simple way of sharing it / to allow access from our main JavaScript logic file as a proof of concept approach.
window.plateBoundariesData = plateBoundariesData;